import { msg } from "@lingui/core/macro";

export const sharedImportedDescriptor = msg`Shared descriptor imported from plain TypeScript.`;

export const sharedImportedDetailDescriptor = msg`Astro, Svelte, and React all translate the same imported descriptor.`;
