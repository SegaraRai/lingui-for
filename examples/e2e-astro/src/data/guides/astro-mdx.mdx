---
title: Astro content collection entry
summary: Render an MDX entry through Astro's content collections pipeline.
---

import { msg, t, Trans } from "lingui-for-astro/macro";

import {
  sharedImportedDescriptor,
  sharedImportedDetailDescriptor,
} from "../../lib/i18n/shared-descriptors.ts";

export const collectionDescriptor = msg`Collection-level MDX descriptors stay extractable.`;

## {t`Content collection rendering`}

{t`Content collections can render MDX entries through entry.render().`}

- {t(sharedImportedDescriptor)}
- {t(sharedImportedDetailDescriptor)}
- {t(collectionDescriptor)}

<aside>{t`HTML tags can render translated MDX expressions too.`}</aside>

<Trans>
  The collection entry keeps the [settings link](/settings) in translated
  output.
</Trans>

<Trans>Collection rich text can preserve **strong emphasis** too.</Trans>
