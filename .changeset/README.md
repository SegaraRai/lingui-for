# Changesets

This directory contains changesets for the lingui-for packages.

To add a changeset, run:

```sh
pnpm changeset
```

See the [Changesets documentation](https://github.com/changesets/changesets) for more details.
