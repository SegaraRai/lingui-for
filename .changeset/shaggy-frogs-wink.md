---
"lingui-for-svelte": patch
"lingui-for-astro": patch
---

Allow using `svelteExtractor` and `astroExtractor` directly in Lingui `extractors` arrays without calling them, while preserving the existing factory form for custom options.
