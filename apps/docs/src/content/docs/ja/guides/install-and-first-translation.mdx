---
title: インストールと最初の翻訳
description: 最初の翻訳メッセージを画面に表示するまでの実践的な手順
---

## 最短手順

1. Lingui とフレームワーク用パッケージをインストールします。
2. アプリが素の `.ts`、`.js`、`.tsx`、`.jsx` で Lingui マクロを使うなら、`unplugin-lingui-macro` を追加します。
3. フレームワーク用の統合またはプラグインを追加します。
4. `lingui.config.js` または `lingui.config.ts` にフレームワーク用抽出器を追加します。
5. ページに `t` または `Trans` を 1 つ置きます。
6. メッセージを増やす前に、まず 1 回だけ抽出とコンパイルを実行します。

## まず適切な記述場所を選ぶ

- `.svelte` では `lingui-for-svelte/macro` から始めます。
- `.astro` では `lingui-for-astro/macro` から始めます。
- 素の JavaScript または TypeScript では `@lingui/core/macro` と `unplugin-lingui-macro` から始めます。
  詳しくは [プレーンな JS/TS でのセットアップ](/ja/guides/plain-js-ts) を参照してください。

## 最初のメッセージの確認項目

- ビルド時にマクロが消えていること
- メッセージがカタログへ抽出されていること
- 対象ロケールで翻訳結果が表示されること

この 3 つのどれかが失敗しているなら、ページを増やす前にその段階を先に調べてください。
